<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.commit.collaboration_board_server.mapper.CommentMapper">

    <insert id="insertComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (content, createdDate, writer, commentId, articleId)
        VALUES (#{content}, NOW(), #{writer}, #{commentId}, #{articleId})
    </insert>

    <select id="selectCommentsByArticleId" resultType="com.commit.collaboration_board_server.model.Comment">
        SELECT * FROM comment
        WHERE articleId = #{articleId}
        ORDER BY createdDate ASC
    </select>

</mapper>